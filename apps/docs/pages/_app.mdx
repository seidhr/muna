import '../styles.css'
import { Noto_Sans_Runic } from 'next/font/google'

export const notoSansRunic = Noto_Sans_Runic({
  subsets: ['runic', 'latin'],
  weight: ['400']
})

export default function App({ Component, pageProps }) {
  return (
    <main className={notoSansRunic.className}>
      <Component {...pageProps} />
    </main>
  )
}
