import { homepage, label, preferredIdentifier } from "../../../properties/datatype"

export default {
  name: 'Dataset',
  type: 'object',
  title: 'Datasett',
  titleEN: 'Dataset',
  fields: [
    homepage,
    label,
    preferredIdentifier
  ],
}
