import { homepage, label, preferredIdentifier } from "../props";

export default {
  name: 'Dataset',
  type: 'object',
  title: 'Datasett',
  titleEN: 'Dataset',
  fields: [
    homepage,
    label,
    preferredIdentifier
  ],
}
